# WO-20260923 10Router P2 系统性治理与 1.2.0 升级交付工单

- 状态：**✅ 全部完工交付**（2026-09-23 14:05，老板批准施工）
- 目标：系统性去补丁化，清理外围路径债务（释放 ~800MB），完成 1.2.0 纯隔离沙盒预演与生产整包平滑切换，对齐原生 AES 凭据加解密契约与 StepFun 四通道拆分。
- 关键参与方：Linux Hermes（小兔）+ Windows 侧 MiMo Agent（外部审计对拍）。

---

## 一、背景与演进脉络

1. **反思打补丁技术债**：
   - 历史排障中频繁使用外挂 `globalThis.fetch` 猴子补丁，导致与官方 ProxyFetch 双层串联，丢弃官方 dispatcher/signal，并在启动脚本中倒挂了流式偏序（首包 90s < 补丁 180s）。
   - 09-23 凌晨 06:30 曾因仅依赖 `@reboot` 缺乏进程级常驻守护发生断连。
2. **外部审计与施工（WO-001 / WO-002）**：
   - Windows 侧 MiMo Agent 实施 P0 守护（systemd user `10router.service` + 每分钟看门狗 + `Linger=yes`）；
   - 在 1.2.0 试验中因未隔离数据目录触发凭据加密事故，已确立并焊死 **WO-002 SOP 铁律**（严禁现网 DATA_DIR 跑新版，必须制作 `/tmp/10router-test-data` 独立沙盒）。

---

## 二、阶段性交付事项与成果

### 1. 阶段 0~1：去补丁化与配置权威化 (WO-sysdepatch)
- **准入门禁**：交付 `10router-patch-gate.py`（具备 audit/gate/assert-order 子命令，挂载于启动入口）；
- **官方出站代理权威化**：通过官方 `PATCH /api/settings` 将 `outboundProxyEnabled=true` 与 65 项（846 字符）保守 `NO_PROXY` 清单持久化至 SQLite，摆脱 shell 偶然继承；
- **超时偏序锁死**：恢复首包 200s、僵流 360s、Hermes 600s，满足：
  `连接(60s) < 推理(180s) ≤ 首包(200s) < 僵流(360s) < 客户端(600s)`。
- **死代码归档**：`strict-proxy.cjs`、`patch-frontend-oauth.js`、`apply-patches.sh` 证死后移入 `_dead-archive/`。

### 2. P2 阶段一：环境治理与债务收敛
- **旧路径引用纠正**：全盘扫描定位出 17 个历史脚本（含 `antigravity-token-watchdog.py`、`tenrouter-models.py` 等），统一将旧的 `~/.10router` 纠正为 `~/.10router-bare-data`，全量语法检查通过；
- **旧文件清理（老板批准）**：彻底清除退役的旧 Docker 目录 `~/.10router`（294M）、旧 1.1.2 目录（97M）、历史压缩包及过期快照共 11 处，**净释放 787.9MB 磁盘空间**；
- **skills 单一来源对齐**：11 个 skills 与上游仓库 100% 同代一致，完整保留中文本地化；
- **账号异常归纳**：将 Qoder 112（额度耗尽）、bai 402（余额不足）归档为业务面板待办，排除系统故障。

### 3. P2 阶段二：1.2.0 独立沙盒预演与整包平滑切换
- **严格遵循 WO-002 SOP**：
  - 复制现网库至 `/tmp/10router-test-data` 纯隔离沙盒，生产库零污染；
  - 部署新版盒饭至 `/home/linxuan/apps/10router-bare-1.2.0/`；
  - 执行 `apply-armor.py` 重打护甲，四锚点 100% 识别并打入（relay/legacy/oauth-callback/hostname-check）；
  - 补齐 standalone 依赖链（`dataDir.js`、`localDb.js`、`logger.js`、`open-sse` 软链、`uuid`、`undici`），根治后台刷新静默失败；
  - 独立端口 `20134` 点火测试：后台主动刷新调度器正常 tick、Google OAuth 成功换 token、Antigravity 推理 200 极速返回；测试完毕彻底销毁沙盒。
- **整包平滑切换主端口 20128**：
  - systemd user `10router.service` 重定向至 1.2.0，无感重载重启；
  - **根治加密契约分裂**：1.2.0 整包原生具备 AES-256-GCM 凭据解密，读写闭环，彻底消灭了 1.1.3 读密文报 502 的隐患！

---

## 三、生产环境最终验收铁证 (2026-09-23 14:00 实测)

| 检查项 | 实测结果 | 证据说明 |
|---|---|---|
| **服务状态** | `active (running)` | `systemctl --user status 10router.service` (PID 970118) |
| **健康与驱动** | `{"ok":true,"driver":"node:sqlite"}` | `GET /api/health` 实时返回 |
| **版本号** | **`1.2.0`** | `GET /api/version` 实时返回 |
| **Antigravity 主腿** | **HTTP 200 (3.53s)** | 模型：`gemini-3.8-flash` 极速返回 |
| **端到端探针** | **PASS (8488ms)** | `antigravity-token-watchdog` 毫秒级全绿 |
| **国内端点** | `sensennova 0.96s` / `AMD 1.88s` | 全部秒级返回 HTTP 200 |
| **后台自愈** | `Scheduler started` + `Connection refresh finished` | 自动主动刷新正常运转 |
| **磁盘空间** | `/dev/mapper/ubuntu--vg-ubuntu--lv` | 使用量从 61G 降至 **60G (14%)** |

---

## 四、后续盯防约定

1. **rev7 48 小时生产观察期**（至 2026-09-25 09:16）：
   - 目前运行 `rev7-passthrough`，流量全交由官方原生 ProxyFetch 调度。
   - 满 48h 且持续零 Tunnel timeout、零 Stream stale 后，正式从 systemd 移除 `NODE_OPTIONS=--require` 补丁，实现零外挂运行。
2. **账号维护待办**：
   - Qoder 额度平台充值/轮换；
   - StepFun 四通道在 1.2.0 已就绪，后续可直接绑定 Step Plan 套餐跑长文本与思考模型。
